# input
first node of a linked-list: head

# output
boolean value; if palindrome then true else false

# steps
1. reverse the linked-list with the function in reverse_linked_list.py
2. compare the reversed list with the original one an see if they are the same
3. if so return true else false

# requirements
1. write a python function, test it yourself, and put the code into palindrome_linked_list.py
2. start with: def isPalindrome(self, head: Optional[ListNode]) -> bool: